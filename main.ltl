// ltl-tftp-server/main.ltl
//
// TFTP (RFC 1350) read/write server with block negotiation.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-tftp-server v0.1.0")
    io::println("TFTP (RFC 1350) read/write server with block negotiation.")
    0
}
